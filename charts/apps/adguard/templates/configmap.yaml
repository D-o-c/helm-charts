{{- include "bjw-s.common.loader.init" . }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "common.global.nameOverride" . }}-config
  labels:
    {{- include "common.labels" . | nindent 4 }}
data:
  AdGuardHome.yaml: |
{{ .Values.config | indent 4 }}
